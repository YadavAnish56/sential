# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
backend/venv/
.pytest_cache/

# Model Weights (Do not commit model weights to Git)
# NOTE: anchored to the repo root so it cannot swallow backend/app/models/ (ORM models).
/models/
*.pt
*.onnx
*.engine
*.bin
*.weights

# Environment & Credentials
.env
*.pem
*.key

# Local Data & Footage (Do not download or commit footage)
*.mp4
*.avi
*.mkv
*.h264
*.h265
postgres_data/

# Local tooling binaries (MediaMTX / FFmpeg) - downloaded per machine, never committed
.tools/

# Local runtime databases
backend/sentinel_local.db
sentinel_local.db
